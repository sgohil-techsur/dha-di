apiVersion: external-secrets.io/v1alpha1
kind: ExternalSecret
metadata:
  name: reservation-secret
spec:
  refreshInterval: "15s"
  secretStoreRef:
    name: k8s-store-local
    kind: SecretStore
  target:
    name: reservation-secret
    template:
      engineVersion: v2
      data: 
        reservation.properties: |
          dbname={{ `{{ .dbname }}` }}
          hostname={{ `{{ .hostname }}` }}
          password={{ `{{ .password }}` }}
          user={{ `{{ .user }}` }}
  data:  
  - secretKey: dbname
    remoteRef:
      key: reservationservice-pguser-reservationservice
      property: dbname  
  - secretKey: hostname
    remoteRef:
      key: reservationservice-pguser-reservationservice
      property: host	
  - secretKey: user
    remoteRef:
      key: reservationservice-pguser-reservationservice
      property: user	
  - secretKey: password
    remoteRef:
      key: reservationservice-pguser-reservationservice
      property: password