apiVersion: external-secrets.io/v1alpha1
kind: ExternalSecret
metadata:
  name: userservice-secret
spec:
  refreshInterval: "15s"
  secretStoreRef:
    name: k8s-store-local
    kind: SecretStore
  target:
    name: userservice-secret
    template:
      engineVersion: v2
      data: 
        userservice.properties: |
          dbname={{ `{{ .dbname }}` }}
          hostname={{ `{{ .hostname }}` }}
          password={{ `{{ .password }}` }}
          user={{ `{{ .user }}` }}
  data:  
  - secretKey: dbname
    remoteRef:
      key: userservice-pguser-userservice
      property: dbname  
  - secretKey: hostname
    remoteRef:
      key: userservice-pguser-userservice
      property: host	
  - secretKey: user
    remoteRef:
      key: userservice-pguser-userservice
      property: user	
  - secretKey: password
    remoteRef:
      key: userservice-pguser-userservice
      property: password